node_modules/
dist/
dist-electron/
.env
.env.local
*.local
service-account*.json
*.pem
.DS_Store
.firebase/

# Grosse generierte Datendateien (via Scripts heruntergeladen)
# sl-data.json ist bewusst NICHT ignoriert: es gibt kein Generator-Skript
# im Repo (BAG-SL-Update laeuft lokal), und deploy-web.yml baute sonst bei
# jedem Push ein Hosting-Deployment OHNE die Datei — die Medikamentensuche
# im Lager zeigte danach "0 Ergebnisse" bis zum naechsten Refdata-Cron.
public/refdata-data.json
public/lieferengpaesse-data.json
public/zurrose-nota-data.json
public/recall-data.json
public/recall-pid-sync.json
public/recall-zu-bearbeiten.json

# Zur Rose XLSX wird lokal heruntergeladen und committed
# (nicht ignorieren!)
# scripts/Nota-Liste.xlsx  ← absichtlich NICHT ignoriert
